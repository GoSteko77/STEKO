/**
 * Base URL path for GitHub Pages subpath hosting.
 * Must match `experiments.baseUrl` in app.json.
 * Used for service worker registration and asset paths.
 */
export const WEB_BASE_URL = "/STEKO";
